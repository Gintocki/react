import React from 'react';
import AuthFormContainers from '../../containers/authForm';

const Auth = () => (
  <section className="auth">
    <AuthFormContainers />
  </section>
);

export default Auth;
